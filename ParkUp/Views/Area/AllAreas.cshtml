@model List<AreaViewModel>

@{
    ViewData["Title"] = "All areas";
}

<h1>All areas</h1>

<p>
    @foreach (var area in Model)
    {
        <span>@area.Name (@area.Id)</span>
    }
</p>

<a class="btn btn-outline-primary" asp-action="AddArea" asp-controller="Area">Add Area</a>
